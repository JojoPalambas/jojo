    * findall_rec(struct accumulator *path)

1 - if path[len - 1] != '/': path += '/'

2 - loop on files of path
2.1 - print path + file_name
2.2 - if file == directory: path += file_name
                            findall_rec(path)

    * findall(char *path)

1 - if path == "": path = "."       // In addition, converts char *path
                                    // to struct accumulator *path

2 - print path

3 - findall_rec(path)

need:
    declare struct accumulator = { char path[4096], size_t pos }
    struct accumulator *accu_init(const char *path)
    struct accumulator *accu_concat(struct accumulator *accu, const char *path)
    void print_concat(struct accumulator *accu, char *path)
