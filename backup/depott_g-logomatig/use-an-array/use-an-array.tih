    type int_array = array of int
    function max_search (tab : int_array, size : int) : int =
        let
            var ret : int := 0
        in
            for i := 0 to size do
                if (tab[i] > tab[ret]) then
                    ret := i ;
            ret
        end
